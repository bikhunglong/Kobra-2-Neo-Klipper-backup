[gcode_macro test]
gcode:
    {% set Z_CALCULATED = printer.save_variables.variables.z_calculate_probe | default(0) | float | round(3) %}
    RESPOND TYPE=command MSG="Z offset moi tinh toan: {Z_CALCULATED}"
